const { Review, User, Products, Sequelize } = require('../models/index');
const { Op } = Sequelize;

const ReviewController = {
  async create(req, res) {
    try {
      const review = '';
    } catch (error) {}
  },
};
